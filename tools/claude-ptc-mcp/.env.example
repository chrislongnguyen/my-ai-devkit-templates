# Copy to .env and fill in. Do not commit .env.
ANTHROPIC_API_KEY=your-anthropic-api-key-here
# Optional: repo root so the server can read files when you pass a scope (e.g. for /heavy).
# WORKSPACE_ROOT=/path/to/effective-build-agent
